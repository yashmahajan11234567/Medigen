# API Specification

POST /schedule/scan
POST /schedule/parse
POST /schedule/validate
POST /schedule/save

Each endpoint should include request, response, validation, and error codes during implementation.
