"use client";
const require_compose_refs = require("../../utils/compose-refs.cjs");
const require_environment_provider = require("../../providers/environment/environment-provider.cjs");
const require_use_safe_layout_effect = require("../../utils/use-safe-layout-effect.cjs");
const require_frame_content = require("./frame-content.cjs");
let react = require("react");
let react_jsx_runtime = require("react/jsx-runtime");
let react_dom = require("react-dom");
//#region src/components/frame/frame.tsx
var initialSrcDoc = `<html><head><style>*,*::before,*::after { margin: 0; padding: 0; box-sizing: border-box; }</style></head><body><div class="frame-root"></div></body></html>`;
function getMountNode(frame) {
	const doc = frame.contentWindow?.document;
	if (!doc) return null;
	return doc.body.querySelector(".frame-root") || doc.body;
}
var Frame = (0, react.forwardRef)((props, ref) => {
	const { children, head, onMount, onUnmount, srcDoc = initialSrcDoc, ...rest } = props;
	const [frameRef, setFrameRef] = (0, react.useState)(null);
	const [mountNode, setMountNode] = (0, react.useState)(null);
	require_use_safe_layout_effect.useSafeLayoutEffect(() => {
		if (!frameRef) return;
		const doc = frameRef.contentWindow?.document;
		if (!doc) return;
		doc.open();
		doc.write(srcDoc);
		doc.close();
		setMountNode(getMountNode(frameRef));
	}, [frameRef, srcDoc]);
	(0, react.useEffect)(() => {
		if (!frameRef?.contentDocument) return;
		const win = frameRef.contentWindow;
		if (!win) return;
		const mountNode = getMountNode(frameRef);
		if (!mountNode) return;
		const exec = () => {
			win.requestAnimationFrame(() => {
				if (!frameRef.contentDocument?.documentElement) return;
				frameRef.style.setProperty("--width", `${mountNode.scrollWidth}px`);
				frameRef.style.setProperty("--height", `${mountNode.scrollHeight}px`);
			});
		};
		const resizeObserver = new win.ResizeObserver(exec);
		exec();
		if (frameRef.contentDocument) resizeObserver.observe(mountNode);
		return () => {
			resizeObserver.disconnect();
		};
	}, [frameRef]);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_environment_provider.EnvironmentProvider, {
		value: () => frameRef?.contentDocument ?? document,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("iframe", {
			title: `frame:${(0, react.useId)()}`,
			ref: require_compose_refs.composeRefs(ref, setFrameRef),
			...rest,
			children: [mountNode ? (0, react_dom.createPortal)(/* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_frame_content.FrameContent, {
				onMount,
				onUnmount,
				children
			}), mountNode) : null, head && frameRef ? (0, react_dom.createPortal)(head, frameRef.contentDocument.head) : null]
		})
	});
});
//#endregion
exports.Frame = Frame;
