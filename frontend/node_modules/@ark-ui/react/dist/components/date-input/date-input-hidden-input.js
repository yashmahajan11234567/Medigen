"use client";
import { createSplitProps } from "../../utils/create-split-props.js";
import { ark } from "../factory.js";
import { useDateInputContext } from "./use-date-input-context.js";
import { forwardRef } from "react";
import { mergeProps } from "@zag-js/react";
import { jsx } from "react/jsx-runtime";
//#region src/components/date-input/date-input-hidden-input.tsx
var splitHiddenInputProps = createSplitProps();
var DateInputHiddenInput = forwardRef((props, ref) => {
	const [hiddenInputProps, localProps] = splitHiddenInputProps(props, ["index", "name"]);
	const mergedProps = mergeProps(useDateInputContext().getHiddenInputProps(hiddenInputProps), localProps);
	return /* @__PURE__ */ jsx(ark.input, {
		...mergedProps,
		ref
	});
});
DateInputHiddenInput.displayName = "DateInputHiddenInput";
//#endregion
export { DateInputHiddenInput };
