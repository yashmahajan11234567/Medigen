"use client";
import { createSplitProps } from "../../utils/create-split-props.js";
import { ark } from "../factory.js";
import { useDateInputContext } from "./use-date-input-context.js";
import { useDateInputSegmentGroupPropsContext } from "./use-date-input-segment-group-props-context.js";
import { forwardRef } from "react";
import { mergeProps } from "@zag-js/react";
import { jsx } from "react/jsx-runtime";
//#region src/components/date-input/date-input-segment.tsx
var splitSegmentProps = createSplitProps();
var DateInputSegment = forwardRef((props, ref) => {
	const [segmentProps, localProps] = splitSegmentProps(props, ["segment"]);
	const segmentGroupProps = useDateInputSegmentGroupPropsContext();
	const mergedProps = mergeProps(useDateInputContext().getSegmentProps({
		...segmentProps,
		index: segmentGroupProps.index
	}), localProps);
	return /* @__PURE__ */ jsx(ark.span, {
		...mergedProps,
		ref,
		children: segmentProps.segment.text
	});
});
DateInputSegment.displayName = "DateInputSegment";
//#endregion
export { DateInputSegment };
