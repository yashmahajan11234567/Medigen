import { Service, EventObject, Machine } from '@zag-js/core';
import { DismissableElementHandlers } from '@zag-js/dismissable';
import { AnimationFrame } from '@zag-js/dom-query';
import { JSX, Point, RequiredBy, DirectionProperty, CommonProperties, MaybeElement, PropTypes } from '@zag-js/types';
import { SwipeSession, PhysicalSwipeDirection } from './utils/session.mjs';

interface ContentPointerDownOptions {
    event: JSX.PointerEvent<HTMLElement>;
    getDoc: () => Document;
    getContentEl: () => HTMLElement | null;
    getWin: () => Window;
    swipeDirection: PhysicalSwipeDirection;
    canCommit: () => boolean;
    onCommit: (point: Point) => void;
}
interface GrabberPointerDownOptions {
    event: JSX.PointerEvent<HTMLElement>;
    point: Point;
    canCommit: () => boolean;
    onCommit: (point: Point) => void;
}
declare class DrawerSwipeSession {
    readonly session: SwipeSession;
    private dragOffset;
    private readonly preventDragOnScroll;
    constructor(options: DrawerSwipeSessionOptions);
    contentPointerDown(options: ContentPointerDownOptions): void;
    grabberPointerDown(options: GrabberPointerDownOptions): void;
    private adjustReleaseVelocityAgainstDisplacement;
    private adjustReleaseVelocityForOpenSwipe;
    beginSwipe(point: Point): void;
    clearSwipeStart(): void;
    getSwipeStart(): Point | null;
    getDragOffset(): number | null;
    resetDragOffset(): void;
    resetVelocity(): void;
    reset(): void;
    setDragOffset(point: Point, resolvedActiveSnapPointOffset: number, direction: PhysicalSwipeDirection): void;
    setSwipeOpenOffset(point: Point, contentSize: number, direction: PhysicalSwipeDirection): void;
    canStartDrag(point: Point, target: EventTarget | null, container: HTMLElement | null, preventDragOnScroll: boolean, direction: PhysicalSwipeDirection): boolean;
    resolveSnapPointOnRelease(snapPoints: ResolvedSnapPoint[], snapPoint: ResolvedSnapPoint | null, snapToSequentialPoints: boolean, contentSize: number): number | string | null;
    shouldOpenOnRelease(contentSize: number | null, swipeVelocityThreshold: number, openThreshold: number): boolean;
    shouldDismissOnRelease(contentSize: number | null, snapPoints: ResolvedSnapPoint[], resolvedSnapOffset: number): boolean;
    getSwipeStrength(targetOffset: number, resolvedSnapOffset?: number | null): number;
    bindDragTracking(options: DragTrackingOptions): () => void;
    bindSwipeOpenTracking(options: SwipeOpenTrackingOptions): () => void;
}
interface DrawerSwipeSessionOptions {
    preventDragOnScroll: () => boolean;
}
interface DragTrackingOptions {
    getDoc: () => Document;
    getContentEl: () => HTMLElement | null;
    getSwipeAreaEl: () => HTMLElement | null;
    swipeDirection: PhysicalSwipeDirection;
    onMove: (details: {
        point: Point;
        target: Element | null;
        swipeDirection: PhysicalSwipeDirection;
    }) => void;
    onEnd: (details: {
        point: Point;
        swipeDirection: PhysicalSwipeDirection;
    }) => void;
    onCancel: () => void;
}
interface SwipeOpenTrackingOptions {
    getDoc: () => Document;
    getContentEl: () => HTMLElement | null;
    getSwipeAreaEl: () => HTMLElement | null;
    swipeDirection: PhysicalSwipeDirection;
    onMove: (details: {
        point: Point;
    }) => void;
    onEnd: (details: {
        point: Point;
        swipeDirection: PhysicalSwipeDirection;
    }) => void;
    onCancel: () => void;
}
declare const oppositeSwipeDirection: Record<SwipeDirection, SwipeDirection>;
declare function resolveSwipeDirection(direction: SwipeDirection, dir?: "ltr" | "rtl"): PhysicalSwipeDirection;
declare function getSwipeDirectionSize(rect: DOMRect, direction: PhysicalSwipeDirection): number;
declare function resolveSwipeProgress(contentSize: number | null, dragOffset: number | null, snapPointOffset: number): number;
declare function hasOpeningSwipeIntent(start: Point, current: Point, direction: PhysicalSwipeDirection): boolean;
declare function isTextSelectionInDrawer(doc: Document, contentEl: HTMLElement | null): boolean;
declare function shouldIgnorePointerDownForDrag(event: JSX.PointerEvent<HTMLElement>): boolean;

type SwipeDirection = "up" | "down" | "start" | "end";
type SnapPoint = number | string;
interface OpenChangeDetails {
    open: boolean;
}
interface TriggerValueChangeDetails {
    value: string | null;
    triggerElement: HTMLElement | null;
}
interface TriggerProps {
    /**
     * The value that identifies this specific trigger
     */
    value?: string;
}
interface ResolvedSnapPoint {
    value: SnapPoint;
    height: number;
    offset: number;
}
interface SnapPointChangeDetails {
    snapPoint: SnapPoint | null;
}
interface NestedDrawerMetrics {
    /**
     * Number of drawers stacked in front of this one.
     */
    count: number;
    /**
     * The measured height of this drawer's content.
     */
    height: number;
    /**
     * The measured height of the frontmost drawer's content.
     */
    frontmostHeight: number;
    /**
     * Whether a nested drawer is currently open in front of this one.
     */
    open: boolean;
    /**
     * Whether a drawer in front of this one is currently being swiped.
     */
    swiping: boolean;
}
interface DrawerStackSnapshot {
    active: boolean;
    openCount: number;
    swipeProgress: number;
    frontmostHeight: number;
}
interface DrawerStack {
    getSnapshot(): DrawerStackSnapshot;
    subscribe(listener: () => void): () => void;
    register(id: string): void;
    unregister(id: string): void;
    setOpen(id: string, open: boolean): void;
    setHeight(id: string, height: number): void;
    setSwipe(id: string, swiping: boolean, progress: number): void;
}
interface DrawerStackApi<T extends PropTypes = PropTypes> {
    getIndentProps(): T["element"];
    getIndentBackgroundProps(): T["element"];
}
type ElementIds = Partial<{
    backdrop: string;
    positioner: string;
    content: string;
    title: string;
    description: string;
    header: string;
    trigger: string | ((value?: string) => string);
    grabber: string;
    grabberIndicator: string;
    closeTrigger: string;
    swipeArea: string;
}>;
interface DrawerProps extends DirectionProperty, CommonProperties, DismissableElementHandlers {
    /**
     * The ids of the elements in the drawer. Useful for composition.
     */
    ids?: ElementIds;
    /**
     * Whether to trap focus inside the sheet when it's opened.
     * @default true
     */
    trapFocus?: boolean | undefined;
    /**
     * Whether to prevent scrolling behind the sheet when it's opened
     * @default true
     */
    preventScroll?: boolean | undefined;
    /**
     * Whether to prevent pointer interaction outside the element and hide all content below it.
     * @default true
     */
    modal?: boolean | undefined;
    /**
     * Element to receive focus when the sheet is opened.
     */
    initialFocusEl?: (() => MaybeElement) | undefined;
    /**
     * Element to receive focus when the sheet is closed.
     */
    finalFocusEl?: (() => MaybeElement) | undefined;
    /**
     * Whether to restore focus to the element that had focus before the sheet was opened.
     * @default true
     */
    restoreFocus?: boolean | undefined;
    /**
     * The sheet's role
     * @default "dialog"
     */
    role?: "dialog" | "alertdialog" | undefined;
    /**
     * The value of the trigger that currently controls the drawer.
     */
    triggerValue?: string | null | undefined;
    /**
     * The default trigger value (uncontrolled).
     */
    defaultTriggerValue?: string | null | undefined;
    /**
     * Callback when the active trigger value changes.
     */
    onTriggerValueChange?: ((details: TriggerValueChangeDetails) => void) | undefined;
    /**
     * Whether the drawer is open.
     */
    open?: boolean | undefined;
    /**
     * The initial open state of the drawer.
     */
    defaultOpen?: boolean | undefined;
    /**
     * Function called when the open state changes.
     */
    onOpenChange?: ((details: OpenChangeDetails) => void) | undefined;
    /**
     * Whether to close the drawer when the outside is clicked.
     * @default true
     */
    closeOnInteractOutside?: boolean | undefined;
    /**
     * Whether to close the drawer when the escape key is pressed.
     * @default true
     */
    closeOnEscape?: boolean | undefined;
    /**
     * The snap points of the drawer.
     * Array of numbers or strings representing the snap points.
     * @default [1]
     */
    snapPoints?: SnapPoint[] | undefined;
    /**
     * The direction in which the drawer can be swiped.
     * @default "down"
     */
    swipeDirection?: SwipeDirection | undefined;
    /**
     * Whether the drawer should snap to sequential points when swiping.
     * @default false
     */
    snapToSequentialPoints?: boolean | undefined;
    /**
     * The threshold velocity (in pixels/s) for closing the drawer.
     * @default 700
     */
    swipeVelocityThreshold?: number;
    /**
     * The threshold distance for dismissing the drawer.
     * @default 0.25
     */
    closeThreshold?: number;
    /**
     * Whether to prevent dragging on scrollable elements.
     * When enabled, the sheet will not start dragging if the user is interacting with a scrollable element.
     * @default true
     */
    preventDragOnScroll?: boolean;
    /**
     * The currently active snap point.
     */
    snapPoint?: SnapPoint | null | undefined;
    /**
     * The default snap point of the drawer.
     * @default 1
     */
    defaultSnapPoint?: SnapPoint | null | undefined;
    /**
     * Callback fired when the snap point changes.
     */
    onSnapPointChange?: ((details: SnapPointChangeDetails) => void) | undefined;
    /**
     * Optional external store for coordinating app-level drawer stack visuals
     * (e.g. indent and background layers).
     */
    stack?: DrawerStack | undefined;
}
type PropsWithDefault = "closeOnInteractOutside" | "closeOnEscape" | "modal" | "trapFocus" | "restoreFocus" | "preventScroll" | "initialFocusEl" | "snapPoints" | "swipeDirection" | "snapToSequentialPoints" | "swipeVelocityThreshold" | "closeThreshold" | "preventDragOnScroll" | "defaultSnapPoint";
interface DrawerSchema {
    props: RequiredBy<DrawerProps, PropsWithDefault>;
    state: "open" | "closed" | "closing" | "swipe-area-dragging" | "swiping-open";
    tag: "open" | "closed";
    context: {
        triggerValue: string | null;
        dragOffset: number | null;
        snapPoint: SnapPoint | null;
        resolvedActiveSnapPoint: ResolvedSnapPoint | null;
        contentSize: number | null;
        viewportSize: number;
        rootFontSize: number;
        swipeStrength: number;
        rendered: {
            title: boolean;
            description: boolean;
        };
        nestedMetrics: NestedDrawerMetrics;
    };
    refs: {
        swipeSession: DrawerSwipeSession;
        snapBackFrame: AnimationFrame;
    };
    computed: {
        drawerId: string;
        physicalSwipeDirection: PhysicalSwipeDirection;
        resolvedSnapPoints: ResolvedSnapPoint[];
    };
    event: EventObject;
    action: string;
    guard: string;
    effect: string;
}
type DrawerService = Service<DrawerSchema>;
type DrawerMachine = Machine<DrawerSchema>;
interface ContentProps {
    /**
     * Whether the drawer content is draggable.
     * If false, the drawer can only be dragged by the grabber.
     * @default true
     */
    draggable?: boolean | undefined;
}
interface SwipeAreaProps {
    /**
     * Whether the swipe area is disabled.
     * @default false
     */
    disabled?: boolean | undefined;
    /**
     * The swipe direction that opens the drawer.
     * Defaults to the opposite of the drawer's `swipeDirection`.
     */
    swipeDirection?: SwipeDirection | undefined;
}
interface DrawerApi<T extends PropTypes = PropTypes> {
    /**
     * Whether the drawer is open.
     */
    open: boolean;
    /**
     * Whether the drawer is currently being dragged.
     */
    dragging: boolean;
    /**
     * The value of the active trigger.
     */
    triggerValue: string | null;
    /**
     * Set the active trigger value.
     */
    setTriggerValue: (value: string | null) => void;
    /**
     * Function to open or close the menu.
     */
    setOpen: (open: boolean) => void;
    /**
     * The snap points of the drawer.
     */
    snapPoints: SnapPoint[];
    /**
     * The swipe direction of the drawer.
     */
    swipeDirection: SwipeDirection;
    /**
     * The currently active snap point.
     */
    snapPoint: SnapPoint | null;
    /**
     * Function to set the active snap point.
     */
    setSnapPoint: (snapPoint: SnapPoint | null) => void;
    /**
     * Get the current open percentage of the drawer.
     * @returns A value between 0-1 where:
     * - 0 = fully closed
     * - 1 = fully open at current snap point
     */
    getOpenPercentage: () => number;
    /**
     * Get the index of the currently active snap point.
     * @returns The index in the snapPoints array, or -1 if not found
     */
    getSnapPointIndex: () => number;
    /**
     * Get the current main-axis size of the drawer content.
     * @returns Size in pixels, or null if not measured yet
     */
    getContentSize: () => number | null;
    getPositionerProps: () => T["element"];
    getContentProps: (props?: ContentProps) => T["element"];
    getTitleProps: () => T["element"];
    getDescriptionProps: () => T["element"];
    getTriggerProps: (props?: TriggerProps) => T["element"];
    getBackdropProps: () => T["element"];
    getGrabberProps: () => T["element"];
    getGrabberIndicatorProps: () => T["element"];
    getCloseTriggerProps: () => T["element"];
    getSwipeAreaProps: (props?: SwipeAreaProps) => T["element"];
}

export { type ContentProps as C, type DrawerApi as D, type ElementIds as E, type NestedDrawerMetrics as N, type OpenChangeDetails as O, type ResolvedSnapPoint as R, type SnapPoint as S, type TriggerProps as T, type DrawerMachine as a, type DrawerProps as b, type DrawerSchema as c, type DrawerService as d, type DrawerStack as e, type DrawerStackApi as f, type DrawerStackSnapshot as g, DrawerSwipeSession as h, type SnapPointChangeDetails as i, type SwipeAreaProps as j, type SwipeDirection as k, type TriggerValueChangeDetails as l, getSwipeDirectionSize as m, hasOpeningSwipeIntent as n, isTextSelectionInDrawer as o, oppositeSwipeDirection as p, resolveSwipeProgress as q, resolveSwipeDirection as r, shouldIgnorePointerDownForDrag as s };
