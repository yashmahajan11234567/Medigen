declare class DrawerRegistry {
    private readonly elements;
    private readonly swipingIds;
    private readonly swipeProgress;
    private readonly listeners;
    notify(): void;
    register(id: string, el: HTMLElement): void;
    unregister(id: string): void;
    setSwiping(id: string, swiping: boolean): void;
    setSwipeProgress(id: string, progress: number): void;
    getSwipeProgressAfter(id: string): number;
    hasSwipingAfter(id: string): boolean;
    getEntries(): Map<string, HTMLElement>;
    subscribe(fn: () => void): () => void;
}
declare const drawerRegistry: DrawerRegistry;

export { drawerRegistry };
