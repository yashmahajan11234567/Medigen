"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/utils/formatting.ts
var formatting_exports = {};
__export(formatting_exports, {
  constrainValues: () => constrainValues,
  createFormatFn: () => createFormatFn,
  getValueAsString: () => getValueAsString,
  resolveHourCycleProp: () => resolveHourCycleProp,
  resolvePlaceholderValue: () => resolvePlaceholderValue
});
module.exports = __toCommonJS(formatting_exports);
var import_date = require("@internationalized/date");
var import_date_utils = require("@zag-js/date-utils");
var import_segments = require("./segments.js");
function getValueAsString(value, prop) {
  return value.map((date) => {
    if (date == null) return "";
    try {
      return prop("format")(date, { locale: prop("locale"), timeZone: prop("timeZone") });
    } catch {
      return "";
    }
  });
}
function constrainValues(values, min, max) {
  return values?.map((date) => (0, import_date_utils.constrainSegments)(date, min, max));
}
function resolveHourCycleProp(hourCycle) {
  if (hourCycle === 12) return "h12";
  if (hourCycle === 24) return "h23";
}
function resolvePlaceholderValue(options, timeZone, granularity, value, defaultValue, calendar) {
  let placeholder = options.placeholderValue || options.defaultPlaceholderValue || value?.[0] || defaultValue?.[0] || (0, import_date_utils.getTodayDate)(timeZone, calendar);
  placeholder = (0, import_date_utils.constrainSegments)(placeholder, options.min, options.max);
  if ((0, import_segments.needsTimeGranularity)(granularity) && !("hour" in placeholder)) {
    placeholder = (0, import_date.toCalendarDateTime)(placeholder);
  }
  return placeholder;
}
function createFormatFn(formatter) {
  return (date, { timeZone }) => {
    const jsd = date.toDate(timeZone);
    const isBC = date.calendar?.identifier === "gregory" && date.era === "BC";
    if (isBC) {
      const prolYear = jsd.getUTCFullYear();
      const safeDate = new Date(Date.UTC(2e3, jsd.getUTCMonth(), jsd.getUTCDate()));
      return formatter.formatToParts(safeDate).map((p) => p.type === "year" ? String(prolYear) : p.value).join("");
    }
    return formatter.format(jsd);
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  constrainValues,
  createFormatFn,
  getValueAsString,
  resolveHourCycleProp,
  resolvePlaceholderValue
});
